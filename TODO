$Id$
- Added method to set config->mango->conrollers-name
- Added rating/s() and comments() to Object using Ratings/Comments models
- Cleanup blessed
- Cleanup loc->localize
- Change admins to use instance instead of load
- fixed instance found checks to if deined
- use chained in index
- factor Handel into ::Handel provider so Mango::Cart/Wishlistcan be dumb
	objects just like the rest
- add fluff columns to products, wishlist items, profiles etc (images, 
	comments, etc)
- test url 404s when substituting ids /wishlist/33333/item/333333333, etc
- test non deletes of other peoples wishlists when passing ids that exist for 
	other users
- clean up Auth/Store/User stuff, esp $user->refreshß
- convert resource names to mango/name
